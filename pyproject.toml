[project]
name = "market-signal-mcp"
version = "0.1.0"
description = "Multi-source market-signal MCP server for case-comp and startup-idea research"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]",
    "pytrends",
]

[tool.uv]
package = false
